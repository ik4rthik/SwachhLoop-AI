# ============================================================
# SwachhLoop AI — Development Dependencies
# ============================================================
# Install with: pip install -r requirements-dev.txt
# (Also install requirements.txt first)

-r requirements.txt

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2          # Used as AsyncClient for FastAPI test client

# --- Linting / Formatting ---
ruff==0.7.4            # Fast Python linter + formatter (replaces flake8 + black)

# --- Type Checking ---
mypy==1.13.0
